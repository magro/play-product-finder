require([ "domReady", "jquery-1.9.1", "ajax_utils", "live_search" ], function(domReady, $, ajaxUtils, liveSearch) {
    // do stuff
    domReady(function() {
        "use strict";
        myApp.AjaxInput.init();
    });
});